// enums used to restrict some of the users choice
const enum SeatChoice {
    AISLE = "aisle", // AISLE =0 (by default)
    MIDDLE = 22,    // default, MIDDLE=1
    WINDOW,   // default, WINDOW=1
    FOURTH    // default, FOURTH=1
}

const akSeat = SeatChoice.AISLE

